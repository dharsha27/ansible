- name: create user 
  hosts: localhost
  connection: local
  become: yes 
  vars:
     user_name: expense
  tasks:
    - name: user name is expense
      user:
        name: "{{ item }}"
      loop:
        - harsha
        - bhargav
        - madhu
        - chari
