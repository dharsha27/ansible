- name: demo on shell command
  hosts: localhost
  connection: local
  tasks:
   - name: command test
     shell: cat /etc/passwd |grep root
     register: users_output

   - name: print output
     debug:
       msg: "{{ users_output }}"