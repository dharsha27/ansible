- name: demo on shell command
  hosts: local
  connection: local
  tasks:
  - name: command test
    shell: cat /etc/passwd |grep root
    registers: users_output

  - name: print output
    debug:
       msg: "{{ users_output }}"