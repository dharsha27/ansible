- name: demo on loops
  hosts: frontend
  become: yes
  tasks:
   - name: install packages
     package:
       name:  "{{ item }}"
       state:  present
     loop:
     - nginx
     - mysql
     - git
     - mongodb-mongosh

   